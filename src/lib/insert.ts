/**
 * 插入内容
 */

const insertTmpty = (params: string) => {
    
}

export const insert = function(data: object) {
    const {el, type, content} = data
    if(type == 'text') {

    }

    if(type == 'image') {

    }

    if(type == 'empty') {
        insertTmpty('2')
    }

    if(type == 'node') {
        
    }
}